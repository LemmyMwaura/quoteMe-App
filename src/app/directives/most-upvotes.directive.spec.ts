import { MostUpvotesDirective } from './most-upvotes.directive';

describe('MostUpvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new MostUpvotesDirective();
    expect(directive).toBeTruthy();
  });
});
